// Reserved for cross-app shared shadcn/ui components.
// Right now @fam/web ships its own copy in apps/web/components/ui to avoid
// premature abstraction; once @fam/admin is added (Этап 3) we'll deduplicate.
export {};
